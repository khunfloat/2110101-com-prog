# print(sum([int(e) for e in input().replace('-', '+-').split('+') if e != '']))

# print(input().replace('-', '+-').split('+'))

# print(sum(map(int, input().replace('-', '0+-').split('+'))))

# print(input().replace('-', '0+-').split('+'))